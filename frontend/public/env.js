// Dev/preview placeholder. In the container this file is never read from disk:
// the static server renders /env.js from the process environment on each
// request, so API_URL can change without rebuilding the bundle.
window.__APP_ENV__ = { API_URL: "" };
